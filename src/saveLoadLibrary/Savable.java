package saveLoadLibrary;

import java.io.Serializable;

/**
 * Maybe make an interface later so that everything that implements savable is saved with 
 * this particular format
 *
 */

public interface Savable extends Serializable {
	
}
